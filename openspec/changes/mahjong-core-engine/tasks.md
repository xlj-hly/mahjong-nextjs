## 1. 项目脚手架与测试框架

- [x] 1.1 安装 Vitest（`npm i -D vitest`）并验证 `package.json` 新增依赖、安装成功
- [x] 1.2 新增 `test` script 与 vitest 配置（含 `@/*` 路径别名），验证 `npm test` 能跑一个空测试套件并退出码为 0
- [x] 1.3 建立 `src/game/core`、`src/game/rules/guobiao`、`src/components` 目录结构，验证目录存在且 `src/game` 不 import 任何 `next`/React 符号

## 2. 引擎核心（src/game/core）

- [x] 2.1 实现牌张模型 `Tile`（`suit` + `rank`，含花牌 suit），验证判等/分组单测通过
- [x] 2.2 实现牌墙模型（构建、洗牌、摸牌、耗尽状态），验证摸牌与耗尽场景单测通过
- [x] 2.3 实现规则无关的面子拆解算法（回溯枚举 N 面子 + 1 对将），验证「标准拆解」「不可和」「多解不重复」单测通过
- [x] 2.4 定义规则插件接口 `RulesPlugin`（tileSet/动作合法性/isWinningHand/calcScore/flow），验证接口类型定义可被国标插件实现且引擎不依赖具体规则
- [x] 2.5 实现行牌状态机骨架（庄家/方位/回合流转/结束判定），验证「回合流转」「和牌结束」单测通过

## 3. 国标规则插件（src/game/rules/guobiao）

- [x] 3.1 实现 144 张牌种集合（万筒条 108 + 风 16 + 箭 12 + 花 8），验证牌墙共 144 张单测通过
- [x] 3.2 实现补花流程（亮花 + 补牌 + 连续补花），验证「摸花补牌」「连续补花」单测通过
- [x] 3.3 实现和牌判定（拆解 + 七对/十三幺/全不靠等特殊牌型），验证标准与特殊牌型和牌判定单测通过
- [x] 3.4 实现番种特征提取（清一色/碰碰和/门前清/刻子顺子/听牌方式等），验证特征提取单测通过
- [x] 3.5 实现 81 番种匹配谓词（1–88 番全覆盖），验证「清一色+碰碰和」「十三幺」等番种识别单测通过
- [x] 3.6 实现不重复原则互斥表，验证「清一色/混一色只计其一」「可叠加番种累加」单测通过
- [x] 3.7 实现 8 番起胡门槛 + 花牌计番 + 线性计分结算，验证「不足 8 番不可和」「自摸/点炮结算」单测通过
- [x] 3.8 （范围假设：首版不含杠分即时结算，仅和牌基础分）验证结算逻辑不涉及杠分，若需纳入将在后续 change 单独处理

## 4. 对局推进与热座模式

- [x] 4.1 实现纯 TS headless 对局运行器（无 React）：持引擎实例 + 国标插件，仅暴露 `snapshot(seat)` 与 `apply({seat,action,tile})` 两个口子；验证「CLI 脚本能纯命令行跑完整一局（发牌→摸打→吃碰杠→和→结算）」且不 import 任何 `next`/React 符号
- [x] 4.2 验证快照/意图契约：`snapshot` 返回纯数据、`apply` 拒绝非法意图，验证「非法操作被拒绝、合法和牌推进」单测通过
- [x] 4.3 实现热座 UI 组件（`TileView` 汉字+CSS 牌块 / `BoardView` 布局与出牌、吃碰杠和按钮）；验证 UI 只消费 `snapshot`、只上报意图，组件内不含合法性或番数判断逻辑
- [x] 4.4 （范围假设：热座入口置于根路由 `/`，替换默认首页）实现根页面挂载热座组件，验证 `npm run dev` 打开首页即进入热座对局

## 5. 集成验证

- [x] 5.1 全量单测、`npm run typecheck`、`npm run lint` 全部通过，验证无回归且覆盖率覆盖拆解/和牌/番种/不重复原则
